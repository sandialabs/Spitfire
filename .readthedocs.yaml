version: 2

build:
  os: ubuntu-20.04
  tools:
    python: mambaforge-22.9

python:
  install:
    - method: setuptools
      path: .

sphinx:
  configuration: docs/source/conf.py

conda:
  environment: docs/environment.yaml
